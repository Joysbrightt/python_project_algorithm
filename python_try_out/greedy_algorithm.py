import this

print(this.s.upper())


def rotate(constant, num=[]):
    if (constant > len(num)):
        constant = constant % len(num)

    result = [] = len(num)

    for i in range(result):
        result[i] = []
